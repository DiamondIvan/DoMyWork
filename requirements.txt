# Python dependencies for the Telegram bot scripts
# Install with: pip install -r requirements.txt

pyrogram>=2.0.0
# Optional but recommended for better performance / fewer crypto warnings
# (works on Windows via wheels in most cases)
# tgcrypto>=1.2.5

# For scheduler_bot.py Firestore enqueue
firebase-admin>=6.5.0

# AI pipeline
google-generativeai>=0.8.0

# Media processing (PDF, Word, image analysis)
pymupdf>=1.24.0
python-docx>=1.1.0
Pillow>=10.0.0
